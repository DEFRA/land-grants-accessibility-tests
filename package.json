{
  "name": "land-grants-accessibility-tests",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "engines": {
    "node": ">=22.13.1"
  },
  "author": "Defra DDTS",
  "license": "OGL-UK-3.0",
  "aliases": {
    "~": ".",
    "page-objects": "test/page-objects",
    "components": "test/components"
  },
  "scripts": {
    "clean": "rm -rf reports",
    "test": "npm run clean && wdio run wdio.conf.js",
    "test:local": "npm run clean && wdio run wdio.local.conf.js",
    "git:pre-commit-hook": "echo 'Pre-commit hook executed successfully'"
  },
  "devDependencies": {
    "@wdio/cli": "9.9.1",
    "@wdio/globals": "9.9.1",
    "@wdio/local-runner": "9.9.1",
    "@wdio/mocha-framework": "9.9.0",
    "@wdio/spec-reporter": "9.9.0",
    "axe-core": "4.10.3",
    "date-fns": "^4.1.0"
  }
}
